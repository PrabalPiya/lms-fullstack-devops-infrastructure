apiVersion: v1
kind: Service
metadata:
  name: lmsbackend
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: lmsbackend
  ports:
    - port: {{ .Values.backend.port }}
      targetPort: {{ .Values.backend.port }}