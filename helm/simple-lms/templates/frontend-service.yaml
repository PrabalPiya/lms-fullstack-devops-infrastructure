apiVersion: v1
kind: Service
metadata:
  name: lmsfrontend
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: lmsfrontend
  ports:
    - port: {{ .Values.frontend.port }}
      targetPort: {{ .Values.frontend.port }}